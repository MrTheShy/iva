<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- The wrist is told, not shown: a tap when listening starts, two when the
         answer lands, a long one on failure. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Android 11+ hides other packages: without this the system speech recognizer
         and text-to-speech engines are invisible to the app. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

</manifest>
