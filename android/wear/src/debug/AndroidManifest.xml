<?xml version="1.0" encoding="utf-8"?>
<!-- Same as the phone app: cleartext only in debug builds, so the watch can be tried
     against a server on the LAN before it has a certificate. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:usesCleartextTraffic="true" />
</manifest>
